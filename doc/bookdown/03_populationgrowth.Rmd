# A chapter on demograpy

Because people have childs who have childs...

$$ 1+1=2 $$

Check with Latex
```{r universaltruth}
1+1==2
```

small tests


$$ y_i = \frac{K}{1 + \left(\frac{K - y_{i-1}}{y_{i-1}}\right)*e^{-r}} $$

A more realistic growth: the environment modulate a carrying capacity K

```{r popgrowth}

K=1000+faketemp*200
N=5
pop=rep(0,tstep)
pop[1]=N
r=rnorm(tstep,.2,.2)
for(i in 2:tstep)
    pop[i]=K[i]/(1 + (( K[i] - pop[i-1])/pop[i-1])*exp(-r[i]))
```

```{r visupopgrowth,fig.cap="logistic growth",out.width="50%"}
    plot(1:tstep,pop,xlab="time",ylab="popsize",main="")
```

